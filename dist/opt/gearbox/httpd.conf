# Replace
# 1. Listen 80 --> #Listen 80
# 2. c:/Apache24 --> ${SERVER_ROOT}
# 3. ${SERVER_ROOT}/htdocs --> ${DEFAULT_DOCUMENT_ROOT}
Include "${SERVER_ROOT}/conf/httpd.conf"

# Include User Config
Include "${CONF_ROOT}/httpd.conf"
